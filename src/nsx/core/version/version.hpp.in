// SPDX-License-Identifier: GPL-3.0-only
//
// GENERATED FILE - do not edit. Source template:
//   src/nsx/core/version/version.hpp.in
// Generated by configure_file() from the values in /VERSION (cmake/NsxVersion.cmake).
//
// This header is the ONLY place a version string exists at runtime. The
// predecessor had five copies (Makefile, curl user-agent, splash footer, tools
// badge, changelog list) and they drifted. If you need the version anywhere,
// include this header - never type a literal. tools/lint/forbid_hardcoded_version.sh
// fails CI on any version literal under src/.

#pragma once

#include <string_view>

/// @brief Build-time identity of this binary.
namespace nsx::core::version {

/// @brief Major version component (SemVer 2.0.0).
inline constexpr int kMajor = @NSX_VERSION_MAJOR@;

/// @brief Minor version component (SemVer 2.0.0).
inline constexpr int kMinor = @NSX_VERSION_MINOR@;

/// @brief Patch version component (SemVer 2.0.0).
inline constexpr int kPatch = @NSX_VERSION_PATCH@;

/// @brief Pre-release identifier without its leading hyphen, empty when absent.
/// @details For `1.0.0-rc.1` this is `rc.1`. Ranks BELOW the same core version
///          during comparison, per SemVer section 11.
inline constexpr std::string_view kPrerelease = "@NSX_VERSION_PRERELEASE@";

/// @brief Full version string with no leading `v`, e.g. `0.1.0` or `1.0.0-rc.1`.
/// @details Matches the release tag with the `v` removed. Displayed in the UI
///          and compared against the update manifest's `version` field.
inline constexpr std::string_view kString = "@NSX_VERSION_RAW@";

/// @brief Abbreviated git commit this binary was built from, `-dirty` when the
///        working tree had uncommitted changes. `unknown` for tarball builds.
inline constexpr std::string_view kGitSha = "@NSX_GIT_SHA@";

/// @brief UTC build date, `YYYY-MM-DD`.
inline constexpr std::string_view kBuildDate = "@NSX_BUILD_DATE@";

/// @brief HTTP `User-Agent` sent with every request.
/// @details GitHub rejects requests that do not send one. Keeping it here means
///          it can never disagree with the displayed version.
inline constexpr std::string_view kUserAgent =
    "NSX-Manager/@NSX_VERSION_RAW@ (Nintendo Switch; libnx)";

/// @brief Product slug used for SD paths, release asset names and the manifest
///        `product` field: `nsx-manager`.
inline constexpr std::string_view kProduct = "nsx-manager";

}  // namespace nsx::core::version
